Hotel room booking (Django + Postgres).

## Run (Docker)

```bash
docker compose up -d --build
```

Open:
- http://localhost:8000/
- http://localhost:8000/admin/

Admin (auto-created):
- username: admin
- password: admin
